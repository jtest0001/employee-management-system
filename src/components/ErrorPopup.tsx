import { ToastContainer } from "react-toastify"

const ErrorPopup = () => (
  <ToastContainer position="top-right" autoClose={3000} closeOnClick={false} draggable={false} theme="colored" />
)

export default ErrorPopup
